module.exports = {
  /**
   * Provide ZipRecruiter's stylelint config as an extensible shared config.
   */
  "extends": "stylelint-config-standard",

  /**
   * The following rules will override the configuration from "extends".
   * https://stylelint.io/user-guide/configuration/#extends
   *
   * A list of stylelint's rules.
   * https://stylelint.io/user-guide/rules/
   *
   * If you change rules:
   *
   * 1. Check if they already exist in ZipRecruiters stylelint configuration and
   *    are already set as desired.
   *    https://git.ziprecruiter.com/ZipRecruiter/ziprecruiter/blob/master/frontend/packages/stylelint-config-ziprecruiter/index.js
   *
   * 2. Please provide a link to the official documentation for the rule, as
   *    well as a comment to describe the reason for it, if appropriate.
   *
   * This is a test area to see how well new rules work. If successful, they
   * will be added to the package.
   */
  "rules": {
    // Add rules here.
  },
};
